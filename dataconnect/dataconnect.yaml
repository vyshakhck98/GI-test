specVersion: "v1beta"
serviceId: "gi-project"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gi-project-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
